<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <contextName>logback</contextName>

    <!--参数-->
    <springProperty scope="context" name="appName" source="spring.application.name" />
    <springProperty scope="context" name="logLevel" source="logging.level.root" />

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule
        conversionWord="wEx"
        converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"
    />

    <!--控制台输出内容的颜色转换以及格式-->
    <substitutionProperty
        name="logging.pattern.console"
        value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
    />
    <!--日志文件输出内容的格式-->
    <substitutionProperty
        name="logging.pattern.file"
        value="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } --- [%t] %logger : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
    />

    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${logLevel}</level>
        </filter>
        <!--控制台使用layout节点-->
        <encoder>
            <Pattern>${logging.pattern.console}</Pattern>
        </encoder>
    </appender>

    <!--按天生成日志-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <Prudent>true</Prudent>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${logLevel}</level>
        </filter>
        <!--滚动策略，我配置了按天生成日志文件-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>logs/%d{yyyy-MM}/${appName}_monitor_info_%d{yyyy-MM-dd}.%i.log</FileNamePattern>
            <maxFileSize>200MB</maxFileSize>
            <maxHistory>100</maxHistory>
        </rollingPolicy>
        <encoder>
            <!--日志中输出日志格式-->
            <!--<outputPatternAsHeader>true</outputPatternAsHeader>-->
            <Pattern>${logging.pattern.file}</Pattern>
        </encoder>
    </appender>

    <springProfile name="dev">
        <root level="${logLevel}">
            <appender-ref ref="console" />
        </root>
    </springProfile>

    <springProfile name="test">
        <root level="${logLevel}">
            <appender-ref ref="console" />
            <appender-ref ref="file" />
        </root>
    </springProfile>

    <springProfile name="prod">
        <root level="${logLevel}">
            <appender-ref ref="file" />
        </root>
    </springProfile>

</configuration>
